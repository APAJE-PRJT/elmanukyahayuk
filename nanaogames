local HttpService=game:GetService("HttpService")local RunService=game:GetService("RunService")local Players=game:GetService("Players")local LocalPlayer=Players.LocalPlayer local WindUI=loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()local Window=WindUI:CreateWindow({Title="EL MANUK NIE DECK",Icon="lucide:mountain-snow",Author="NanaoGames",Folder="BRDNHub",Size=UDim2.fromOffset(580,460),Theme="Midnight",Resizable=true,SideBarWidth=200,})local POSITION_ALPHA=0.5 local VELOCITY_ALPHA=0.5 local replayData={}local isPlaying=false local currentFrame=1 local playbackSpeed=1 local connection=nil local lastPlaybackState=nil local frameTimeAccumulator=0 local routesLoaded=false if _G.__FLIP_MODE==nil then _G.__FLIP_MODE=false end _G.__ROTATION_OFFSET_Y=0 _G.__PB_START_TIME=0 _G.__PB_START_TIMESTAMP=0 local CACHE_FOLDER="TrackCache" local function ensureCacheFolder()if not isfolder then return false end if not isfolder(CACHE_FOLDER)then makefolder(CACHE_FOLDER)end return true end local function getCacheFileName(url)local hash=string.gsub(url,"[^%w]","_")return CACHE_FOLDER .. "/" .. hash .. ".json" end local function loadFromCache(url)local cacheFile=getCacheFileName(url)if not readfile or not isfile or not isfile(cacheFile)then return nil end local success,result=pcall(function()local content=readfile(cacheFile)local data=HttpService:JSONDecode(content)if type(data)=="table" and #data>0 then return data end return nil end)if success then return result end return nil end local function saveToCache(url,data)if not writefile then return false end local success=pcall(function()ensureCacheFolder()local cacheFile=getCacheFileName(url)local jsonData=HttpService:JSONEncode(data)writefile(cacheFile,jsonData)end)return success end local function downloadRoute(url)local body=nil local success,result=pcall(function()return game:HttpGet(url)end)if success and result then body=result end if not body then local request_fn=(syn and syn.request)or(http and http.request)or(http_request)or(fluxus and fluxus.request)or(request)if request_fn then local s2,r2=pcall(function()local resp=request_fn({Url=url,Method="GET"})if resp and resp.Body then return resp.Body end return nil end)if s2 and r2 then body=r2 end end end if not body or body=="" then warn("[Track]Download failed-empty response")return nil end local data=nil local parseOk=false parseOk,data=pcall(function()return HttpService:JSONDecode(body)end)if not parseOk then parseOk,data=pcall(function()return loadstring(body)()end)end if parseOk and type(data)=="table" and #data>0 then saveToCache(url,data)return data end warn("[Track]Failed to parse route data. Make sure the link returns a valid JSON Array or Lua Table.")return nil end local function loadRoute(url)local cachedData=loadFromCache(url)if cachedData then return cachedData end return downloadRoute(url)end local function forceRedownloadRoute(url)local cacheFile=getCacheFileName(url)if delfile and isfile and isfile(cacheFile)then pcall(function()delfile(cacheFile)end)end return downloadRoute(url)end local routeUrls={{"EL MANUK NIE DECK","https://raw.githubusercontent.com/APAJE-PRJT/trackyahayukv3/refs/heads/main/n"},}local routes={}local function loadAllRoutes()routes={}local loadedCount=0 WindUI:Notify({Title="Loading Routes",Content="Downloading track data...",Duration=2,Icon="download"})for i,routeInfo in ipairs(routeUrls)do local routeName=routeInfo[1]local routeUrl=routeInfo[2]local routeData=loadRoute(routeUrl)if routeData and #routeData>0 then table.insert(routes,{routeName,routeData})loadedCount=loadedCount+1 else warn(string.format("Failed to load route: %s",routeName))end end if loadedCount>0 then routesLoaded=true WindUI:Notify({Title="Routes Loaded",Content=string.format("Successfully loaded %d/%d routes",loadedCount,#routeUrls),Duration=3,Icon="check"})return true else routesLoaded=false WindUI:Notify({Title="Load Failed",Content="Could not load any routes!",Duration=3,Icon="x"})return false end end local function reloadAllRoutes()routes={}routesLoaded=false WindUI:Notify({Title="Reloading Routes",Content="Force downloading...",Duration=2,Icon="refresh-cw"})local loadedCount=0 for i,routeInfo in ipairs(routeUrls)do local routeName=routeInfo[1]local routeUrl=routeInfo[2]local routeData=forceRedownloadRoute(routeUrl)if routeData and #routeData>0 then table.insert(routes,{routeName,routeData})loadedCount=loadedCount+1 end end if loadedCount>0 then routesLoaded=true WindUI:Notify({Title="Routes Reloaded",Content=string.format("Successfully reloaded %d/%d routes",loadedCount,#routeUrls),Duration=3,Icon="check"})return true else routesLoaded=false WindUI:Notify({Title="Reload Failed",Content="Could not reload any routes!",Duration=3,Icon="x"})return false end end local function GetFrameCFrame(frame,humanoid)if not frame then return CFrame.new()end local y=frame.Position[2]if frame.HipHeight and humanoid then local recordedHipHeight=frame.HipHeight local currentHipHeight=humanoid.HipHeight local offset=currentHipHeight-recordedHipHeight y=y+offset end local pos=Vector3.new(frame.Position[1],y,frame.Position[3])local look=Vector3.new(frame.LookVector[1],frame.LookVector[2],frame.LookVector[3])local up=Vector3.new(frame.UpVector[1],frame.UpVector[2],frame.UpVector[3])or Vector3.new(0,1,0)local cf=CFrame.lookAt(pos,pos+look,up)if _G.__FLIP_MODE then cf=cf*CFrame.Angles(0,math.rad(180),0)end if _G.__ROTATION_OFFSET_Y~=0 then cf=cf*CFrame.Angles(0,math.rad(_G.__ROTATION_OFFSET_Y),0)end return cf end local function GetFrameVelocity(frame)if not frame or not frame.Velocity then return Vector3.new(0,0,0)end return Vector3.new(frame.Velocity[1],frame.Velocity[2],frame.Velocity[3])end local function ApplyFrameDirect(frame,humanoidRootPart,humanoid)local success,err=pcall(function()if not frame or not humanoidRootPart or not humanoid then return end local moveState=frame.MoveState or "Grounded" local velocity=GetFrameVelocity(frame)local verticalVelocity=velocity.Y if verticalVelocity>5 then moveState="Jumping" elseif verticalVelocity<-5 then moveState="Falling" end humanoid.WalkSpeed=(frame.WalkSpeed or 16)*playbackSpeed if _G.__FLIP_MODE then humanoid.AutoRotate=false else humanoid.AutoRotate=true end humanoid.PlatformStand=false if moveState=="Jumping" then if lastPlaybackState~="Jumping" then humanoid:ChangeState(Enum.HumanoidStateType.Jumping)lastPlaybackState="Jumping" end elseif moveState=="Falling" then if lastPlaybackState~="Falling" then humanoid:ChangeState(Enum.HumanoidStateType.Freefall)lastPlaybackState="Falling" end else if lastPlaybackState~="Grounded" then humanoid:ChangeState(Enum.HumanoidStateType.Running)lastPlaybackState="Grounded" end end local targetCF=GetFrameCFrame(frame,humanoid)humanoidRootPart.CFrame=humanoidRootPart.CFrame:Lerp(targetCF,POSITION_ALPHA)if moveState~="Jumping" then humanoidRootPart.AssemblyLinearVelocity=humanoidRootPart.AssemblyLinearVelocity:Lerp(velocity,VELOCITY_ALPHA)else humanoidRootPart.AssemblyLinearVelocity=humanoidRootPart.AssemblyLinearVelocity:Lerp(velocity,VELOCITY_ALPHA)end humanoidRootPart.AssemblyAngularVelocity=Vector3.zero end)if not success then warn("[Replay]Error frame: " .. tostring(err))end end local function getNearestFrameIndex(frames)local char=LocalPlayer.Character if not char then return 1 end local hrp=char:FindFirstChild("HumanoidRootPart")local hum=char:FindFirstChildOfClass("Humanoid")if not hrp or not hum then return 1 end local startIdx,dist=1,math.huge local pos=hrp.Position for i,frame in ipairs(frames)do if frame then local frameCF=GetFrameCFrame(frame,hum)local framePos=frameCF.Position local d=(framePos-pos).Magnitude if d<dist then dist=d startIdx=i end end end if startIdx>=#frames then startIdx=math.max(1,#frames-1)end return startIdx end function stopPlayback()isPlaying=false if connection then connection:Disconnect()connection=nil end lastPlaybackState=nil frameTimeAccumulator=0 _G.__PB_START_TIME=0 _G.__PB_START_TIMESTAMP=0 local character=LocalPlayer.Character if character then local humanoid=character:FindFirstChildOfClass("Humanoid")if humanoid then humanoid.AutoRotate=true humanoid.WalkSpeed=16 humanoid.PlatformStand=false end local hrp=character:FindFirstChild("HumanoidRootPart")if hrp then hrp.AssemblyLinearVelocity=Vector3.zero hrp.AssemblyAngularVelocity=Vector3.zero end end end function startPlayback()if not routesLoaded or #routes==0 then WindUI:Notify({Title="Error",Content="Please load routes first using the LOAD button!",Duration=3,Icon="alert-triangle"})return end local character=LocalPlayer.Character if not character then WindUI:Notify({Title="Error",Content="Character not found!",Duration=3,Icon="user-x"})return end local humanoidRootPart=character:FindFirstChild("HumanoidRootPart")local humanoid=character:FindFirstChildOfClass("Humanoid")if not humanoidRootPart or not humanoid then WindUI:Notify({Title="Error",Content="Character parts missing!",Duration=3,Icon="alert-circle"})return end if connection then connection:Disconnect();connection=nil end isPlaying=true lastPlaybackState=nil frameTimeAccumulator=0 local frames=routes[1][2]if #frames<2 then WindUI:Notify({Title="Error",Content="Not enough frames!",Duration=3,Icon="x"})return end currentFrame=getNearestFrameIndex(frames)WindUI:Notify({Title="Replay Started",Content=string.format("Playing from frame %d at %.2fx speed",currentFrame,playbackSpeed),Duration=2,Icon="play"})if frames[currentFrame]then humanoidRootPart.CFrame=GetFrameCFrame(frames[currentFrame],humanoid)humanoidRootPart.AssemblyLinearVelocity=Vector3.zero humanoidRootPart.AssemblyAngularVelocity=Vector3.zero task.wait(0.05)end if _G.__PB_START_TIME==0 or not _G.__PB_START_TIME then _G.__PB_START_TIME=tick()end if _G.__PB_START_TIMESTAMP==0 or not _G.__PB_START_TIMESTAMP then _G.__PB_START_TIMESTAMP=frames[currentFrame].Timestamp or 0 end connection=RunService.Heartbeat:Connect(function(deltaTime)if not isPlaying then if connection then connection:Disconnect();connection=nil end return end local char=LocalPlayer.Character if not char then stopPlayback();return end local hrp=char:FindFirstChild("HumanoidRootPart")local hum=char:FindFirstChildOfClass("Humanoid")if not hrp or not hum then stopPlayback();return end local fpsMultiplier=_G.__FPS_MULTIPLIER or 1 local effectiveSpeed=playbackSpeed*fpsMultiplier local timeElapsed=(tick()-_G.__PB_START_TIME)*effectiveSpeed local targetTimestamp if _G.__REVERSE_MODE then targetTimestamp=_G.__PB_START_TIMESTAMP-timeElapsed else targetTimestamp=_G.__PB_START_TIMESTAMP+timeElapsed end local targetFrame=currentFrame if _G.__REVERSE_MODE then for i=currentFrame,1,-1 do if frames[i]and frames[i].Timestamp<=targetTimestamp then targetFrame=i;break end end else for i=currentFrame,#frames do if frames[i]and frames[i].Timestamp>=targetTimestamp then targetFrame=i;break end end end if targetFrame~=currentFrame then local startFrame=currentFrame local endFrame=targetFrame local step=1 if _G.__REVERSE_MODE then startFrame=currentFrame endFrame=targetFrame step=-1 end local framesApplied=0 local maxFramesPerTick=10 for i=startFrame,endFrame,step do if framesApplied>=maxFramesPerTick then break end if i>=1 and i<=#frames then local frame=frames[i]if frame then ApplyFrameDirect(frame,hrp,hum)framesApplied=framesApplied+1 end end end currentFrame=targetFrame end local framesRemaining=#frames-currentFrame local reachedEnd=false if not _G.__REVERSE_MODE then if framesRemaining<10 then reachedEnd=true end else if currentFrame<10 then reachedEnd=true end end if reachedEnd then if connection then connection:Disconnect();connection=nil end if not _G.__REVERSE_MODE then currentFrame=1 else currentFrame=#frames end if hrp and hum and frames[currentFrame]then hrp.CFrame=GetFrameCFrame(frames[currentFrame],hum)hrp.AssemblyLinearVelocity=Vector3.zero hrp.AssemblyAngularVelocity=Vector3.zero end _G.__PB_START_TIME=tick()_G.__PB_START_TIMESTAMP=frames[currentFrame].Timestamp or 0 WindUI:Notify({Title="Looping",Content="Restarting replay...",Duration=1,Icon="repeat"})task.wait(0.1)if not isPlaying then return end task.spawn(function()startPlayback()end)return end end)end function runAllRoutes()if not routesLoaded or #routes==0 then WindUI:Notify({Title="Error",Content="Please load routes first using the LOAD button!",Duration=3,Icon="alert-triangle"})return end isPlaying=true while isPlaying do local char=LocalPlayer.Character if not char then break end for r=1,#routes do if not isPlaying then break end local frames=routes[r][2]if #frames<2 then continue end local startIdx=getNearestFrameIndex(frames)for i=startIdx,#frames-1 do if not isPlaying then break end local hrp=char:FindFirstChild("HumanoidRootPart")local hum=char:FindFirstChildOfClass("Humanoid")if not hrp or not hum then break end ApplyFrameDirect(frames[i],hrp,hum)task.wait(1/60/playbackSpeed)end end if not isPlaying then break end LocalPlayer.Character:BreakJoints()task.wait(5)end end local MainTab=Window:Tab({Title="Main",Icon="geist:shareplay",Default=true})local speeds={}for v=0.25,3,0.25 do table.insert(speeds,string.format("%.2fx",v))end MainTab:Dropdown({Title="Speed",Icon="lucide:zap",Values=speeds,Value="1.00x",Callback=function(option)local num=tonumber(option:match("([%d%.]+)"))if num then playbackSpeed=num end end})MainTab:Toggle({Title="Interval Flip",Icon="lucide:refresh-ccw",Desc="ON → Hadap belakang tiap frame",Value=false,Callback=function(state)_G.__FLIP_MODE=state end})MainTab:Toggle({Title="Anti Beton Ultra-Smooth",Icon="lucide:shield",Desc="Mencegah jatuh kaku",Value=false,Callback=function(state)end})MainTab:Button({Title="LOAD ROUTES",Icon="lucide:download",Desc="Download track dari URL",Callback=function()task.spawn(loadAllRoutes)end})MainTab:Button({Title="RELOAD ROUTES",Icon="lucide:refresh-cw",Desc="Force download ulang(clear cache)",Callback=function()task.spawn(reloadAllRoutes)end})MainTab:Button({Title="START",Icon="craft:back-to-start-stroke",Desc="Mulai dari checkpoint terdekat",Callback=function()pcall(startPlayback)end})MainTab:Button({Title="AWAL KE AKHIR",Desc="Jalankan semua checkpoint",Icon="craft:back-to-start-stroke",Callback=function()pcall(runAllRoutes)end})MainTab:Button({Title="Stop track",Icon="geist:stop-circle",Desc="Hentikan route",Callback=function()pcall(stopPlayback)end})local SettingsTab=Window:Tab({Title="Tools",Icon="geist:settings-sliders",})SettingsTab:Button({Title="ANIMASI",Icon="lucide:layers-2",Callback=function()loadstring(game:HttpGet("https://bantaigunung.my.id/rawku.php?api=3672637846&file=ANIMASI.json"))()end})SettingsTab:Slider({Title="WalkSpeed",Icon="lucide:zap",Value={Min=10,Max=500,Default=16},Step=1,Suffix="Speed",Callback=function(val)local char=LocalPlayer.Character if char and char:FindFirstChild("Humanoid")then char.Humanoid.WalkSpeed=val end end})SettingsTab:Slider({Title="Jump Height",Icon="lucide:zap",Value={Min=10,Max=500,Default=50},Step=1,Suffix="Height",Callback=function(val)local char=LocalPlayer.Character if char and char:FindFirstChild("Humanoid")then char.Humanoid.JumpPower=val end end})SettingsTab:Button({Title="Respawn Player",Icon="lucide:user-minus",Callback=function()LocalPlayer.Character:BreakJoints()end})SettingsTab:Button({Title="Speed Coil",Icon="lucide:zap",Callback=function()local speedValue=23 local function giveCoil(char)local backpack=LocalPlayer:WaitForChild("Backpack")if backpack:FindFirstChild("Speed Coil")or char:FindFirstChild("Speed Coil")then return end local tool=Instance.new("Tool")tool.Name="Speed Coil" tool.RequiresHandle=false tool.Parent=backpack tool.Equipped:Connect(function()local humanoid=char:FindFirstChildOfClass("Humanoid")if humanoid then humanoid.WalkSpeed=speedValue end end)tool.Unequipped:Connect(function()local humanoid=char:FindFirstChildOfClass("Humanoid")if humanoid then humanoid.WalkSpeed=16 end end)end if LocalPlayer.Character then giveCoil(LocalPlayer.Character)end LocalPlayer.CharacterAdded:Connect(function(char)task.wait(1)giveCoil(char)end)end})SettingsTab:Button({Title="TP Tool",Icon="lucide:chevrons-up-down",Callback=function()local mouse=LocalPlayer:GetMouse()local tool=Instance.new("Tool")tool.RequiresHandle=false tool.Name="Teleport" tool.Parent=LocalPlayer.Backpack tool.Activated:Connect(function()if mouse.Hit then local char=LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart")then char.HumanoidRootPart.CFrame=CFrame.new(mouse.Hit.Position+Vector3.new(0,3,0))end end end)end})SettingsTab:Button({Title="Gling GUI",Icon="lucide:layers-2",Callback=function()loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Fling-Gui-Op-47914"))()end})local InfoTab=Window:Tab({Title="Info",Icon="lucide:info",})InfoTab:Section({Title="INFO SC",TextSize=20})InfoTab:Section({Title=[[Replay/route system untuk checkpoint.-Start CP=mulai dari checkpoint terdekat-Start To End=jalankan semua checkpoint-Playback Speed=atur kecepatan replay(0.25x-3.00x)@NanaoGames]],TextSize=16,TextTransparency=0.25,})Window:DisableTopbarButtons({"Close"})Window:EditOpenButton({Title="EL MANUK NIE DECK",Icon="geist:logo-nuxt",CornerRadius=UDim.new(0,16),StrokeThickness=2,Color=ColorSequence.new(Color3.fromHex("FF0F7B"),Color3.fromHex("F89B29")),OnlyMobile=false,Enabled=true,Draggable=true,})Window:Tag({Title="V2.0",Color=Color3.fromHex("#30ff6a"),Radius=10,})WindUI:Notify({Title="EL MANUK NIE DECK",Content="Script loaded!",Duration=3})pcall(function()Window:Show()MainTab:Show()end)
